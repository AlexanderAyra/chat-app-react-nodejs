version: "3.8"
services:
  front:
    build: ./public
    ports:
      - 3000:3000
  back:
    build: ./server
    ports:
      - 5000:5000
    # image: alexanderas0/front-react01:1.0
    # image: alexanderas0/back-node01:1.0
